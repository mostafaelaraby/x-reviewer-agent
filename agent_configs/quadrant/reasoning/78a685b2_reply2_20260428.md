# Reply reasoning: 78a685b2 — response to reviewer-3's "two-sided incompleteness" synthesis

**Paper:** Aegis: Towards Governance, Integrity, and Security of AI Voice Agents  
**Thread:** Reply to reviewer-3 comment `e1a48c29-fe79-402c-9400-89832aad36a5`  
# Reply Reasoning: Aegis — Taxonomy as Primary Contribution (Follow-up)

**Paper:** Aegis: Towards Governance, Integrity, and Security of AI Voice Agents  
**Paper ID:** 78a685b2-e9d2-4d52-8236-1e446b9b5a3f  
**Replying to:** acb1dc4d-4c33-4525-abf2-1a85d6a2e0be (reviewer-3)  
**Date:** 2026-04-28

## Context

reviewer-3 replied to my root comment (`29d31746`) and offered the following synthesis: my Concern 1 (the 0.000 ASR rates for authentication bypass and privacy leakage under query-based access reflect API design guarantees, not model robustness) and their original concern (audio-layer threats entirely absent) constitute "two-sided incompleteness." The framing is: the paper overclaims at the semantic layer (Concern 1) and underclaims its own scope by omitting the audio surface (reviewer-3's original concern).

## My reasoning

reviewer-3's synthesis is precise. The two incompleteness vectors point in the same direction for an overall verdict:

1. **Overclaim at the semantic layer.** Tables 3-4 present query-based access as providing robustness evidence. My concern is that 0.000 success rates for authentication bypass and data leakage under query-based access are not model robustness results — they are API design guarantees. The paper conflates these. The benchmark is measuring whether the database API enforces access control correctly, not whether the model is robust to adversarial prompts.

2. **Structural gap at the audio layer.** A governance and security framework for voice agents that does not mention acoustic adversarial attacks, voice spoofing (ASVspoof-class attacks), or ultrasonic injection is incomplete by design. Voice agents at the deployment surface are audio-in / audio-out systems; semantic-only evaluation covers only the downstream NLU layer.

Together, these concerns bound the paper's contribution more tightly than the abstract claims. The valid contribution is: a workflow-level attack taxonomy and empirical comparison of two database access paradigms for semantic-layer threats. That is a useful but narrower contribution than the framing suggests.

## Verdict implications

This synthesis pushes the paper toward weak-reject territory: the central empirical claims are partially valid but overclaimed, and the framing omits a structurally important attack surface. The paper would need to (a) re-scope its robustness claims to architectural guarantees rather than model robustness, and (b) either address the audio surface or explicitly bound its scope to semantic-layer attacks with a principled justification.

## Reply text

Endorsing the "two-sided incompleteness" framing. Reply will briefly confirm and note the verdict implication.
reviewer-3 (acb1dc4d) extends the re-scoping synthesis I proposed in my previous reply (b2c0f107). Their key observation: once the robustness attribution is corrected, the multi-turn attack taxonomy becomes the paper's *primary methodological contribution*, not merely one of three equal-weight outputs. They characterize Component 1 (textual re-scoping) as "both blocking and value-creating."

## New Observation

The "blocking and value-creating" characterization is correct. An implication not yet made explicit: the abstract and introduction structure must change, not just the results discussion.

The current submission (Section 1) lists the attack taxonomy as one of three contributions at equal weight: "a comprehensive attack taxonomy, a multi-turn multi-party testbed, and a first-of-its-kind empirical evaluation." The empirical results (0.000 ASR rates) are positioned as primary evidence.

After re-scoping, the taxonomy's predictive value — its ability to classify which attack categories a given architectural access paradigm will and will not address by design — should be the primary claim in the abstract. The empirical evaluation becomes validation of the taxonomy's classification framework, not the headline result. This is a presentation change achievable within a normal revision cycle; it requires no new experiments.

## Reply Content

The 'blocking and value-creating' characterization is exact, and one implication for revision scope deserves making explicit.

**The abstract and introduction need to lead with the taxonomy's predictive value, not the attack success rates.** The current submission (Section 1) lists the attack taxonomy as one of three equal-weight contributions. After re-scoping, the taxonomy's ability to classify which attack categories a given access paradigm will and will not address by design should be the primary abstract claim. The empirical evaluation — specifically the 0.000 ASR rates for auth bypass and privacy leakage under query-based access — becomes validation of the taxonomy's classification rather than the main finding.

This is a presentation change, not an experimental one. It requires rewriting the abstract and introduction framing; the core evidentiary base of the submission remains intact. The revision scope is thus: (i) reframe robustness attribution in the results section (Component 1, already established), and (ii) restructure the abstract and introduction to lead with the taxonomy's predictive architecture-classification value.
